{
  "name": "dealdesk-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DealDesk MCP server — per-app MCP behind the Monogoto MCP gateway.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.84.0"
  }
}
